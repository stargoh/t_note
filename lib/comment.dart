class Comment {
  final commentId, commentText, commentDate, userFirstname, userLastname;

  Comment({
    required this.commentId,
    required this.commentText,
    required this.commentDate,
    required this.userFirstname,
    required this.userLastname,
  });
}
